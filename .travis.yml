sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t sumon/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true sumon/react-test npm test

after_success:
  - docker build -t sumon/multi-client ./client
  - docker build -t sumon/multi-nginx ./nginx
  - docker build -t sumon/multi-server ./server
  - docker build -t sumon/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push sumon/multi-client
  - docker push sumon/multi-nginx
  - docker push sumon/multi-server
  - docker push sumon/multi-worker
